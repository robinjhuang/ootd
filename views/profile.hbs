<h1>Welcome, {{user.local.email}}</h1>

<div class="container">
	<div class = "row">
		<div class="col-sm-12">
			{{#each user.outfits}}
				<img class="img-responsive" src="{{this.image.link}}">
				{{/each}}
		</div>
	</div>
</div>